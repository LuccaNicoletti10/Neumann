{
  "extends": "../../tsconfig.node.json",
  "compilerOptions": {
    // WHY: this tsconfig is typecheck-only; emit lives on tsconfig.build.json
    "noEmit": true
  },
  "include": ["src","tests"]
}
