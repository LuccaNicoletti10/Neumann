{
  "extends": "../../tsconfig.node.json",
  "compilerOptions": {
    // WHY: this package compiled without verbatim imports; enabling it is a separate type-fix, not a tooling preset change
    "verbatimModuleSyntax": false,
    // WHY: exactOptionalPropertyTypes was already true; dropping it would reduce strictness
    "exactOptionalPropertyTypes": true,
    // WHY: this package already emitted without source maps; changing output requires a build evidence pass
    "sourceMap": false
  },
  "include": ["src","tests"]
}
