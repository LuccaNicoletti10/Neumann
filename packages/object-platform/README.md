# object-platform (Passo 18)

Mapping versionado `dataset → ObjectType` + projetor + Object API.

- **US 8,930,897** — property/link mapping físico → ontology
- **US 10,691,729 / EP3425537A1** — object platform (store + history + API)
- **US 11,816,156** — mapping declarativo campo→termo (sem NLP/NVK)
- **US 12,561,339** — `queryObjects` unificada (sem multi-DB físico)

```bash
pnpm obj -- demo
# aliases: objects / object-platform
```

Skip neste passo: GUI, multi object-databases, vector-clock completo, NVK ranking.
