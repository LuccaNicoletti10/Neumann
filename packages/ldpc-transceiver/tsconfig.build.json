{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "dist",
    // WHY: emit must not include tests
    "rootDir": "src"
  },
  "include": ["src/**/*.ts"],
  "exclude": ["tests"]
}
