{
  "name": "common-build-system",
  "version": "1.0.0",
  "description": "Common Build System para monorepo — implementação independente baseada no comportamento da patente US 10,001,982",
  "license": "MIT",
  "type": "commonjs",
  "engines": {
    "node": "^24.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "cli": "node dist/cli.js",
    "api": "node dist/server/index.js"
  },
  "dependencies": {
    "fastify": "catalog:",
    "pino": "9.3.2",
    "zod": "3.23.8"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "@vitest/coverage-v8": "catalog:"
  }
}
