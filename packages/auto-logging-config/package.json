{
  "name": "auto-logging-config",
  "version": "1.0.0",
  "description": "Configuracao automatica de infraestrutura de logging a partir de codigo-fonte (implementacao funcional independente baseada na US 11,681,606 B2)",
  "license": "MIT",
  "type": "commonjs",
  "main": "dist/cli.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "cli": "node dist/cli.js",
    "api": "node dist/server/index.js"
  },
  "dependencies": {
    "fastify": "catalog:",
    "pino": "^9.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "@vitest/coverage-v8": "catalog:"
  }
}
